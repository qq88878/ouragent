spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edu?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    database: 0
  mail:
    host: smtp.qq.com
    port: 587
    username: 2370584562@qq.com
    password: nvhsvheognthebib
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

agent:
  service:
    url: http://localhost:8000
    key: default-dev-key
    timeout: 10000
    retry:
      max-attempts: 3
      delay: 1000

logging:
  level:
    com.edu.agent: DEBUG

file:
  upload:
    path: ./uploads
    allowed-types: pdf,docx,md,txt

app:
  base-url: http://localhost:3000
# ============================================================
# Agent Service - Docker Compose
# 一键启动: docker-compose up -d
# ============================================================

services:
  # --- Agent Service ---
  agent-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-service
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - config/.env
    environment:
      # 应用配置
      - APP_ENV=production
      - DEBUG=false
      - LOG_LEVEL=INFO

      # Redis 配置
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}

      # 安全配置
      - SECRET_KEY=${SECRET_KEY:-change-this-in-production}
      - AGENT_SERVICE_KEY=${AGENT_SERVICE_KEY:-change-this-in-production}
    volumes:
      - agent-data:/app/data
      - agent-logs:/app/logs
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - agent-network

  # --- Redis ---
  redis:
    image: redis:7-alpine
    container_name: agent-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - agent-network

volumes:
  agent-data:
    driver: local
  agent-logs:
    driver: local
  redis-data:
    driver: local

networks:
  agent-network:
    driver: bridge
